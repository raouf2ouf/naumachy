# Decisions log

Short, dated, one line each. Newest at the top of each list. Anyone's AI session reads this before deciding something a teammate could disagree with.

## Decided

- 2026-09-05 Name: Naumachy (submission and repo). Rudis is the prize and the closing line. Aquascan is the instrument. naumachy.eth, .xyz, .fi, GitHub and npm were free at check time.
- 2026-09-05 Partner picks: 1inch, The Graph, Ledger. Uniswap dropped (peripheral in a 1inch-first project). ENS is the alternate to Ledger if Key Ring proves unusable.
- 2026-09-05 One submission, one monorepo. Aquascan's chain data comes from The Graph only. Prices and derived metrics in an enrichment service, disclosed.
- 2026-09-05 Gym on an anvil fork with a local graph-node; arena live on Base with small real bankrolls, indexed by the network.
- 2026-09-05 Only economic fills count, everywhere, including the arena scorer.
- 2026-09-05 Ownership by vertical: Raouf = Rudis (contracts, agents, arena, Ledger); Kate = Aquascan (subgraphs, substreams, aquascan, mcp, video script, feedback docs). Equal skills assumed.
- 2026-09-05 License: MIT for our code; 1inch repos as submodules under Degensoft 1.1.
- 2026-09-05 Submodule pins: `swap-vm` v1.0.2 (the release line the deployed routers run and the published `@1inch/swap-vm-sdk` 0.4.1 encodes for; `main` moved to a sparse 256-slot opcode numbering the SDK does not use), `aqua` v1.0.0 (interface identical to 0.1.0), `forge-std` v1.16.2.
- 2026-09-05 Opcode bytes are positional: the table's first static slot becomes the array length word, so runtime byte = static index minus one (full router: jump 0x0a, dynamicBalances 0x12, salt 0x22, last 0x2d; Aqua router: concentrate 0x12, last 0x21). SDK 0.4.1 encodes exactly this. Custom opcodes are appended after the last slot by overriding `_opcodes()` in `NaumachyRouter`, so the first custom byte is 0x2e. On the SDK side the same byte falls out of a `ProgramBuilder` subclass whose instruction list is the SDK's full list plus ours.

- 2026-09-06 Chain roster for the Aqua subgraph: all six chains where Aqua has activity (Ethereum, BSC, Polygon, Base, Arbitrum, Optimism). One codebase, one manifest per network from a shared template. Publish order: Ethereum first, Base second, the other four as one batch once the mappings are proven on Ethereum, so a mapping bug never costs six resyncs.
- 2026-09-06 Both Aqua registries are indexed. The canonical registry `0x1111113ccf1426a8e30e2bff5e005d929bf6a90a` (July 2026, the one 1inch documents) and the legacy registry `0x499943e74fb0ce105688beee8ef2abec5d936d31` (active since November 2025; fills on Ethereum, Base and Arbitrum only, verified on chain). Same ABI and handlers as a second data source, a `registry` field on Strategy and Fill, the explorer defaults to canonical and badges legacy. Start block per chain and registry is the deployment block.

- 2026-09-06 Prices. No oracle subgraph: an on-chain price subgraph would mean one data source per feed per chain, aggregator upgrades to track, and thin coverage of the long tail Aqua makers trade. Three kinds of price instead. Pair-native prices and 24h VWAP marks come from a strategy's own fills in the Aqua subgraph and need no oracle; the arena scores on these. Dollar prices per token and hour come from DefiLlama as the primary source, which blends venues itself, with a negative cache for uncovered hours. Markout references come from The Graph Token API's hourly candles on the deepest pool of the exact pair a desk quotes; pool choice is a written rule (known factories, minimum age and activity, direct stable pool else one hop through WETH, median of qualifying pools, cross-checked against DefiLlama), stored in a routes table and overridable. Every priced number carries provenance and confidence; a token with no acceptable price stays unpriced rather than wrong. Granularity is hourly; no sub-hour markouts.

- 2026-09-07 Reference prices come from the venue's own tape. For a two-sided fill the reference is the volume-weighted price of the same pair's other fills (other makers and other takers only) in the narrowest window around the fill minute that holds one, up to 15 minutes; the later references at 5 minutes, 1 hour and 1 day widen the same way. A reference must sit within a factor of two of the fill's own price and prints under half a dollar do not count. Dollars still come from the hourly price of the pair's quote token, which only scales a residual. Fills with no such reference fall back to the hourly dollar prices and say so. Measured before deciding: the Chainlink 1INCH/USD feed updates on a 1% deviation (186 times in 30 days), its Data Stream was never verified on chain in 30 days, the Uniswap 1INCH pools do $62K a month against Aqua's $21M a day, and Binance 1INCH/USDT is thin (half its minutes empty) and drifts from the venue by tens of bps for days. Same-pair pool candles from the Token API are dropped: the pools are empty.
- 2026-09-07 Markout is the standard one: the fill re-marked at the reference price a horizon later, equal to edge plus drift. The rollup used to store the drift alone under that name and the pages presented it as the re-marked figure; on Ethereum over 30 days that understated the one-hour picture by half. Drift (markout minus edge) stays as its own column and ranks the most adverse flow.
- 2026-09-07 The headline is the fill re-marked five minutes later, shown as "edge, 5 min after fill". The fill-time edge compares a quote with other quotes that had not moved yet; a taker trades exactly when they are stale, so the fill-time number flatters. Five minutes later the market has re-priced and the same comparison is honest. The fill-time edge stays beside it. Later references start at the horizon and run forward (never back to the fill) and exclude the maker's own later prints.
- 2026-09-07 Every headline rate carries its precision: the volume-weighted markout in bps and one standard error from the spread of the desk's own fills (delta method on the ratio). Measured against Binance's minute prices, the tape reference sits 21 bps away for the median stable-quoted 1INCH fill, 72 bps at the 90th percentile, unbiased and no tighter at 5 minutes or 1 hour than at the fill: that gap is the venue-versus-world basis, not a source to buy away. Precision on a desk comes from its number of fills, and the band says so.
- 2026-09-07 Pairs whose own tape is thin are priced from same-chain pools, never from an exchange. A route is decided per pair once a day: the tape when the pair prints at least ten times an hour; else the deepest pool of the exact pair that traded at least 120 times in 30 days; else a hop through a hub (WETH, USDC, USDT; WBNB on BSC) with the dense leg on the tape and the other leg in a pool; else hourly prices. Pools and their swaps come from published DEX subgraphs with the Uniswap v3 schema through the gateway, Ethereum and BSC today; the Base, Arbitrum, Optimism and Polygon candidates either use another schema or had no healthy indexer when tried, and those chains carry under $30K a month, so they keep hourly references until a subgraph is found. Every number says which route priced it. Three guards apply to every print before it can serve as a reference: a print further from its pair's yardstick for the hour (the hourly dollar ratio, else the pair's own median) than half a percent between stables, two percent between majors or five otherwise is not a market price; a reference needs at least two prints; and it must sit within a factor of 1.4 of the fill's own price. The maker's own prints are removed on tape legs only.
- 2026-09-07 M1 opcode pick. ToxicityFee first (`0x2e`), RiskCap second (`0x2f`), plus OracleAnchor (`0x30`) because the upstream `OraclePriceAdjuster1D` turned out to only ever improve the taker's side of a limit order, which is the opposite of anchoring a market maker; it is dispatched at `0x31` for completeness. FlashRebalance is dropped for good: Aqua moves the maker's outgoing token with a plain `transferFrom` and offers no maker hook, so nothing can be borrowed inside a fill. ArenaExtruction is dropped: the arena reads the chain, the chain does not read the arena. The first gladiator is `RiskCap > ToxicityFee > OracleAnchor > XYCSwap > Salt`, anchored on a `PoolPriceOracle` over the Base WETH/USDC 0.05% pool, the same pool Aquascan scores against. Go/no-go passed on a fork of Base against the canonical registry: nine tests, real transfers. The router is 23,660 bytes: the full table plus three instructions exceeded Base's 24,576-byte limit, so the slots no archetype composes (pegged swap, experimental fee variants, invalidators, gas adjuster) keep their bytes and revert.
- 2026-09-07 ArenaRegistry is a ledger of attestations, not a scorer. Gladiators register and enter their Aqua strategies from their own wallets; the lanista opens and closes generations, attests one score per entry as Aquascan computed it (5-minute markouts in the quote token, with the standard error and the fill count), names the champion, and promotes. Nothing on chain recomputes a score, so the contract stays small (3.7 KB) and the truth lives in the subgraphs, where anyone can check it.
- 2026-09-07 Arena score: the sum of 5-minute markouts in the quote token, with its band, over economic fills; the fill-time edge shown beside it, the close-marked P&L as the survival figure. The flow mix of the taker engine is calibrated to the mainnet corpus first; a dial later.
- 2026-09-07 Fees are read from the program bytes and shown everywhere. The flat maker fee stays with the maker (the taker's grossed-up amount is pushed in full), so it is inside the edge; the protocol fee is pulled out of the maker's ledger in the same transaction, so every net is after it. Fee base is 1e9 in the deployed routers. 94.6% of Ethereum volume runs at 0.10 bps with a 0.025 bps protocol cut.
- 2026-09-07 The mind is one call per gladiator per generation, to the Claude API (`claude-opus-5`, adaptive thinking, a structured answer checked against a schema), and it chooses the knobs of the anchored archetype, never bytes: fee at rest, slope, ceiling, window, depth, cap, plus which program it mutates and a rationale in prose. Knobs are public (the program is bytes on chain and the archetype decodes them); the rationale is private to the gladiator that wrote it. Both are kept under `infra/data/gym/generations/<generation>-<address>.json` with the program, the blob, what the private fork quoted, and what the gladiator saw. Two generations of a line differ by the diff of two small files.
- 2026-09-07 The validator runs on a private anvil forked from the gym at its head, not on an `evm_snapshot`/`evm_revert` of the gym: a revert there would roll back the engine's fills under graph-node and Aquascan, which would then hold phantom fills. The draft is shipped and quoted both ways on the private fork at a twentieth of its ledger; a reverting quote (a cap too tight, an empty curve, a bad argument) rejects the draft before anything reaches the gym. The fork dies either way.
- 2026-09-07 A control line beside the minds: `GLADIATOR_MIND=heuristic` replaces the call with a deterministic hill climber that copies the last champion's knobs and moves one of them by a fixed factor. Explicit only, named in its rationale, so a generation can say whether the mind earns anything over blind mutation. It also runs the whole chain when no API key is present.
- 2026-09-07 The verdict of record is the attested score, what the lanista wrote on chain at close. Aquascan's live numbers for the same strategy keep moving while it stays shipped (generation 0 ranked differently two hours after close for that reason); the context shows both and names them. A gladiator docks its previous program before shipping the next, so the engine trades one program per wallet per generation.
- 2026-09-07 The uninformed flow shops. One order per tick at random (direction, size, and a tolerance drawn from an exponential with mean 10 bps); every gladiator is quoted at the order's size and the best quote wins the order if it beats the pool after the pool's 5 bps fee, or if it is within the taker's tolerance of the mid; otherwise the pool trades. Before this, each gladiator received its own takes at any price, so the highest fee won every generation (generation 3). Mainnet's takers are 1inch's resolvers, who route to the best price; the tolerance models the few who do not. Knobs: `TOLERANCE_BPS`, `POOL_FEE_BPS`, `NOISE_PROBABILITY` (now per order, not per gladiator; the gym runs 0.9 and 12 bps so four losing programs still collect enough fills to be scored).

- 2026-09-08 Correction. SwapVM's maker hooks (`IMakerHooks`: pre and post transfer in and out, `MakerTraits` bits 252 to 245, target any contract or the maker itself) fire in Aqua mode; verified on a fork of Base with a maker holding no WETH that filled a 100 USDC buy from a vault through its pre-transfer-out hook and swept the USDC back through post-transfer-in. The 09-07 line "Aqua offers no maker hook, nothing can be borrowed inside a fill" was wrong: the hook lives in SwapVM's settlement, not in Aqua. FlashRebalance stays dropped, for a different reason: just-in-time inventory is custody, not pricing, LOP v4's pre-interaction gives the same, and a hook target is a contract per maker, which dilutes "a strategy is bytes". A hollow maker with a vault is a Ledger garnish, not a gate mover.
- 2026-09-08 One inventory, three markets. `OracleAnchor` carries a table of pairs (63 bytes each, three fit the 255-byte argument) and a strategy ships WETH, USDC and cbBTC together; every pair is quoted from one ledger at its own pool. The claim a pool cannot match, with its hazard named: a taker can loop through the three quotes, and a reference 1% off on one pair pays the loop about 1% less three fees (test on the fork). The validator runs a 50 USDC loop on the private fork and refuses a program the loop pays.
- 2026-09-08 The arena pass. `ArenaPass` is an ERC20 the lanista mints; a program that opens with `OnlyTakerTokenBalanceNonZero(pass)` serves only takers holding one. The gym runs two takers: one with a pass (routed flow) and one without (the raider); `FLOW_PASS_SHARE` (0.6) of uninformed orders and `INFORMED_PASS_SHARE` (0.2) of informed ones arrive with a pass, so a gate trades toxic flow away together with benign flow. The engine quotes as the taker that would take, so the gate answers each differently. The same gate keeps 99% of mainnet Aqua volume for 1inch's resolvers.
- 2026-09-08 The minds write programs, not knobs. A program is pairs, a cap, and an ordered instruction list from the dialect menu: gate, flat fee, toxicity fee, fees by the token the taker pays (compiled to `JumpIfTokenIn` branches around the middleware, each branch jumping to the shared tail), anchor with its depth, and the swap, last. `arena/src/program.ts` compiles it (RiskCap first, never the mind's to drop; Salt last), lists it, and reads bytes back so rivals' programs reach every mind as listings. The compiler and the private-fork validator refuse a program with a sentence, which goes back to the mind once (`GLADIATOR_ATTEMPTS`). Order matters and is the mind's: a fee before the anchor measures pressure on the real balance, after it on the virtual one. Decay and concentrated ranges stay out of the menu until their interplay with the anchor is understood.
- 2026-09-08 The Arena page prints each program as compiled, its draft prices per pair, the triangle loop's result and any refused drafts, from the generation file.

- 2026-09-08 (evening, Raouf) Authoring order within a generation is accepted and documented. Minds write in roster order and a program is public the moment it ships, as on the venue; the open generation's entries in a mind's briefing are the rivals who wrote before it this round, and the ones after it will see its program before they write. Nothing is hidden from anyone; the order is the arena's. Seen 2026-09-08: "tight" answered "steady" dropping its gate in generation 2 and copied it in generation 3. The minds are told their position by the system prompt. Rejected: rotating the order, hiding the open generation, simultaneous authoring.
- 2026-09-08 (evening, Raouf) The record versus the running total, by wording. Two minds kept re-shipping the generation 0 recipe because Aquascan's desk figures (which accumulate across generations and fields) looked larger than the attested verdicts. The system prompt and the `aquascan` tool now say that the verdict of record is the attested score of a generation against that generation's field, and that desk and leaderboard figures accumulate. No information is removed.
- 2026-09-08 (evening, Raouf) The informed taker reads the tape ahead. The old trigger (front-run a tape swap of at least $20K) fired 4 times in 667 fills, so toxicity fees were never tested and the gate protected against nothing; ungated programs won by keeping the passless 40% of benign flow. Now the engine knows each pair's price `INFORMED_HORIZON_S` (300) ahead from the tape's own swaps (median of the prints around the horizon) and, with `INFORMED_PROBABILITY` (0.03) per tick and pair, trades a coming move of at least `INFORMED_MOVE_BPS` (5) against every gladiator on the pair at `INFORMED_USD` (100); at 0.1 and $150 the first minutes of a trending tape put four dollars of informed flow on the makers for every benign one, measured 2026-09-08, through the raider mostly (`INFORMED_PASS_SHARE`). Fills are marked five minutes later at the moved price, which is what the score measures. The pools keep moving from noise once the tape is exhausted; long seasons set `TAPE_MINUTES` to cover them.

## Assumed (proceeding, not yet confirmed)

- Live arena chain = Base. Alternate: Arbitrum.
- A network-published subgraph on Base indexing ArenaRegistry counts as live Graph data.
- The canonical Base Aqua router is the 16-opcode AquaSwapVMRouter; NaumachyRouter is a modified redeployment of the full router.
- Tenderly virtual testnets count as local forks for 1inch. We use anvil regardless.

- 2026-09-06 Enrichment rollup, taken while building, to review: markout references are the hourly dollar prices at fill hour plus one hour and one day (DefiLlama), the same source as the edge, so the two are comparable; same-pair pool candles from the Token API are the planned refinement, not the first cut. Volume of a fill is the dollar value of what the maker received at the fill hour. The numeraire is a stablecoin when the strategy touched one (matched on symbol against a fixed list in `aquascan/enrich/src/rollup.ts`), else its most traded token, ties broken by address. The rollup recomputes every derived table in one transaction after each pass; with a quarter million fills it takes under half a minute, so incremental rollups can wait.

## Open


- Which custom opcode(s) ship first: RiskCap, FlashRebalance, ToxicityFee, ArenaExtruction. Decided at M1.
- Bankroll per gladiator on Base.
- Whether the taker on Base is only our engine or also real takers (real takers are the truest fitness function; state it in the video either way).
- ENS lineage subnames: only if it costs nothing after M5.
- Lineage per entry. `ArenaRegistry.register` takes a parent once, at birth; a mind that copies a rival in generation 3 records that parent in its generation file, not on chain. Adding `parent` to `enter` (and to the arena subgraph's `Entry`) makes lineage a tree of programs rather than of wallets. Decide before the arena contract goes to Base.
- The hold-out tape is a label for now: `openGeneration` alternates a train and a hold-out tape id, and the engine's tape replay does not yet read the open generation's id to pick its segment. Real once the network pools subgraph reaches the fork block and the tape is live.
